{
  "name": "mobile",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "@expo/metro-runtime": "~57.0.15",
    "expo": "^57.0.22",
    "expo-asset": "~57.0.17",
    "expo-status-bar": "~57.0.1",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-native": "0.86.3",
    "react-native-safe-area-context": "~5.7.0",
    "react-native-web": "^0.21.0",
    "react-native-webview": "13.16.1"
  },
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "build:android": "npx eas-cli build -p android --profile production",
    "build:android:apk": "npx eas-cli build -p android --profile preview",
    "build:ios": "npx eas-cli build -p ios --profile production",
    "build:all": "npx eas-cli build --all --profile production",
    "submit:android": "npx eas-cli submit -p android",
    "submit:ios": "npx eas-cli submit -p ios"
  },
  "private": true
}
